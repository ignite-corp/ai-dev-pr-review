# Probe file for AT-2206

Probe file for AT-2206; this PR is throwaway.

It exists only so that a `.github/lens-ignore` pattern has something to exclude.

This second probe commit changes only this file, so every file in the diff against the base branch is excluded by the policy.
The expected outcome is a policy-driven skip of the review, not a review of an empty diff.
